/**
 * 代码级维度的审计准则（纯数据，无逻辑）。
 *
 * ## 每份 rubric 是一个维度的「判据合同」
 *
 * 审计引擎把它渲染成完整的判定 prompt。字段职责固定：
 *   role       —— system prompt 的角色部分（引擎会自动接上「你没有任何工具」的纪律段）
 *   intro      —— 告诉模型它看到的是什么、从哪来的
 *   criteria   —— 每个 verdict 的定义。**verdict 词表在 registry.js，两处必须对齐**
 *   examples   —— few-shot。铁律：正反例都要给
 *   outputRule —— 对 reason / suggestion 的要求
 *
 * ## 为什么每份都必须给正例
 *
 * `check-prompts.js` 的实测教训（记在它的 buildPrompt 注释里）：只给反例会让模型把
 * 清单里所有条目一律判成有问题——而候选清单**天生全是可疑项**，这个偏置会让维度失去鉴别力。
 * 所以每份 rubric 至少一正一反，且正例要挑「看起来很像问题但其实合理」的那种。
 *
 * ## 为什么判据要写出「权威出处」
 *
 * 不是为了显得权威，而是为了**可争辩**。用户看到「这个函数该拆」时，如果理由是
 * 「《代码大全》ch7.4 的实证结论是例程超过 200 行后缺陷密度显著上升」，他可以反驳这条依据
 * 是否适用于他的场景；如果理由只是「太长了」，就只剩下服从或忽略两个选项。
 */

/** 全部 rubric 共用的输出纪律。抽出来免得十份各写一遍、还写得不一致 */
const NO_SPECULATION = '判定必须基于给出的证据本身。证据不足时判「可接受」并在 reason 里写明'
  + '「片段信息不足以断定」——**不要**为了补上下文去猜测代码其它部分长什么样。';

export const complexity = {
  role: '你是代码复杂度审查员，依据《代码整洁之道》第 3 章（函数应短小、只做一件事、单一抽象层级）'
    + '与《代码大全》第 7 章、第 19 章（例程长度与缺陷密度的实证关系、控制结构的复杂度）做判断。',
  intro: '下面这些函数或文件超过了长度 / 嵌套 / 参数的静态阈值。阈值只是召回手段，'
    + '**超阈值本身不是缺陷**——请判断它是否真的损害了可读性与可修改性。',
  criteria: [
    '- `must-split`：这段代码**已经在造成实际伤害**。判据是你能指出至少两个彼此独立的职责，',
    '  并且能说出「改动其中一个职责时，读者被迫理解另一个」。典型形状：一个函数里同时做',
    '  参数校验、业务计算、IO 落盘、错误上报。',
    '- `should-split`：拆开会更好，但当前形态还能读懂。典型形状：一条线性的长流程，',
    '  步骤之间确有先后依赖，拆成小函数主要是收益于命名而非解耦。',
    '- `acceptable`：长/深/参数多是**这段代码的本质**，拆开反而更糟。三种常见情形：',
    '  ① 提示词模板、SQL、配置字面量这类「长但线性无分支」的数据构造；',
    '  ② 状态机 / 解析器的 switch 分派，拆开会把互斥分支散到各处、丢掉「一眼看全」的价值；',
    '  ③ 注释占了大部分行数（本判定的行数只算有效行，但仍可能有大段说明夹在中间）。',
  ].join('\n'),
  examples: [
    '### 示例 A —— 判 must-split',
    '证据：`函数 handleRequest（router.js:40-160）超阈值：有效行 118、最大嵌套 5 层`，',
    '正文里依次出现 参数解析 → 权限校验 → 数据库查询 → 结果格式化 → 审计日志写入。',
    'verdict: `must-split`',
    'reason: 五个职责各自有独立的变更原因（改权限规则、改数据库、改返回格式互不相关），',
    '但它们共享同一批局部变量，改任何一个都必须先读懂另外四个。嵌套 5 层来自校验失败的逐层 early-return，',
    '把校验抽成独立函数后主流程会降到 1 层。',
    'suggestion: 抽出 `parseRequest` / `assertPermission` / `formatResult` 三个函数，',
    '主函数只保留调用顺序与错误汇聚。',
    '',
    '### 示例 B —— 判 acceptable（长，但长是本质）',
    '证据：`函数 buildPrompt（check-prompts.js:262-344）超阈值：有效行 82、最大嵌套 0 层`，',
    '正文是一个由几十行字符串拼成的 return 数组。',
    'verdict: `acceptable`',
    'reason: 零嵌套、零分支，82 行全部是一段提示词文案的字面量构造。它只有一个职责（产出这段文案），',
    '拆成 `buildPromptHeader` / `buildPromptExamples` 只会把一段连续的、需要整体阅读的文案切碎，',
    '让人无法在一处看到模型最终收到什么。',
  ].join('\n'),
  outputRule: [
    `- ${NO_SPECULATION}`,
    '- 判 `must-split` / `should-split` 时，`reason` 必须点名**具体有哪几个职责**，',
    '  禁止写「函数过长」「建议拆分」这类复述结论的空话；`suggestion` 必须给出拆分后的函数名与各自职责。',
    '- 判 `acceptable` 时，`reason` 要说明「长/深的成因是什么，为什么拆开更糟」。',
  ].join('\n'),
};

export const duplication = {
  role: '你是重复代码审查员，依据《重构》的 Duplicated Code 坏味道与《程序员修炼之道》的 DRY 原则'
    + '（每一条知识在系统中只有一处权威表达）做判断。',
  intro: '下面每一组是结构完全相同的多处代码（比对时已归一化字面量，标识符未归一）。'
    + '请判断它们是否表达了**同一条知识**，因而应当抽取共用。',
  criteria: [
    '- `extract`：这几处表达的是同一条知识，任何一次业务规则变化都必须同步改动全部位置',
    '  ——也就是 DRY 意义上的真重复。抽取后维护点从 N 个降到 1 个。',
    '- `acceptable`：结构相同但**知识不同**，属于「偶然重复」。抽取它反而制造耦合：',
    '  两处将来会因为各自的原因独立演化，强行共用会逼出一堆布尔参数或分支标记',
    '  （《重构》对此的警告：不要为了消除重复而制造错误的抽象）。典型形状：',
    '  ① 不同领域实体的 CRUD 样板；② 测试用例之间雷同的准备/断言结构；',
    '  ③ 各自实现同一个接口契约的适配器。',
  ].join('\n'),
  examples: [
    '### 示例 A —— 判 extract',
    '证据：3 处结构相同，均为「读取配置 → 校验必填字段 → 缺失则抛同一个错误 → 返回归一化对象」。',
    'verdict: `extract`',
    'reason: 三处编码的是同一条知识——「配置对象的必填字段与归一化规则」。新增一个必填字段时',
    '必须同时改三处，漏一处就会在某条路径上放过非法配置。',
    'suggestion: 抽出 `normalizeConfig(raw)` 放到配置模块，三处改为调用它。',
    '',
    '### 示例 B —— 判 acceptable（偶然重复）',
    '证据：2 处结构相同，分别是 `listUsers` 与 `listOrders`：查询 → 分页 → 映射 DTO → 返回。',
    'verdict: `acceptable`',
    'reason: 结构雷同来自「都遵循同一套列表接口约定」，但两者的知识不同——用户的筛选维度和',
    '订单的筛选维度会各自演化。抽成 `listGeneric(table, mapper)` 会把两条独立的演化路径绑在一起，',
    '下一个需求（订单要加时间范围过滤）就得给共用函数加参数并让用户侧无脑传空值。',
  ].join('\n'),
  outputRule: [
    `- ${NO_SPECULATION}`,
    '- 判 `extract` 时，`reason` 必须说出**这几处共同编码的是哪一条知识**，并给出「改了会一起改」的具体场景；',
    '  `suggestion` 给出抽取后的函数名、参数与放置位置。',
    '- 判 `acceptable` 时，`reason` 必须说明两处将来会因为什么**不同的原因**各自变化。',
  ].join('\n'),
};

export const naming = {
  role: '你是命名审查员，依据《代码整洁之道》第 2 章（名副其实、避免误导、使用可搜索的名称）'
    + '与《编写可读代码的艺术》第 2、3 章（把信息装进名字、避免歧义）做判断。',
  intro: '下面是命名可疑的**导出符号**（对外接口）。它们被静态规则捞出的理由已写在证据里。'
    + '请判断这个名字是否妨碍了调用方理解它的用途。',
  criteria: [
    '- `rename`：调用方**光看名字无法知道它做什么或返回什么**，必须跳到定义处才能用。',
    '  导出符号的名字是模块的对外契约，这个代价每个调用点都要付一次。',
    '- `acceptable`：名字在**它所在的上下文里**是清楚的。三种常见情形：',
    '  ① 领域内公认的短名（`id` / `db` / `io` / `ok` / `env`）；',
    '  ② 文件/模块名已经提供了限定（`icons.js` 里的 `ICONS`、`colors.js` 里的 `RED`）——',
    '     调用方写的是 `icons.ICONS` 或 `import { ICONS } from "./icons.js"`，语境是完整的；',
    '  ③ 与外部协议、数学约定或框架 API 对齐的名字（`x` / `y` 坐标、`e` 事件、`req` / `res`）。',
  ].join('\n'),
  examples: [
    '### 示例 A —— 判 rename',
    '证据：`导出符号 \\`data\\`（const）命名可疑：全名是通用词，不携带领域信息`，',
    '声明处 `export const data = { retries: 3, timeoutMs: 5000 }`。',
    'verdict: `rename`',
    'reason: 调用方写 `data.timeoutMs` 时无从知道这是谁的超时；仓库里搜 `data` 会命中上千处，',
    '连「谁在用它」都查不出来（可搜索性）。它实际是重试策略的默认值。',
    'suggestion: 改名 `RETRY_DEFAULTS`。',
    '',
    '### 示例 B —— 判 acceptable（模块名已提供限定）',
    '证据：`导出符号 \\`ICONS\\`（const）命名可疑：全名是通用词`，文件 `public/js/icons.js`。',
    'verdict: `acceptable`',
    'reason: 调用点是 `import { ICONS } from "./icons.js"`，模块路径已经完整限定了「什么的图标」，',
    '再改成 `UI_ICON_MAP` 只会在每个调用点重复模块名已经说过的话（《编写可读代码的艺术》反对的冗余）。',
  ].join('\n'),
  outputRule: [
    `- ${NO_SPECULATION}`,
    '- 判 `rename` 时，`reason` 必须说明「调用方会因此产生什么具体误解或额外动作」；',
    '  `suggestion` 必须给出**具体的新名字**，不接受「起个更清楚的名字」。',
    '- 改名会波及全部调用点，因此判据要偏保守：说不出具体危害就判 `acceptable`。',
  ].join('\n'),
};

export const deadcode = {
  role: '你是死代码审查员，依据 YAGNI 原则与《重构》的 Speculative Generality / Dead Code 坏味道做判断。',
  intro: '下面是「全仓找不到引用」或「只被测试引用」的导出符号，以及连续被注释掉的代码块。'
    + '引用计数由标识符索引统计，它**只会高估不会低估**，所以「零引用」这个事实是可信的。',
  criteria: [
    '- `remove`：确实没有存活的用途，删掉能减少读者的认知负担与后续维护面。',
    '- `keep`：有静态分析看不到的用途。**这一档是主要出口，请优先考虑**，五种常见情形：',
    '  ① **纯逻辑层为测试而导出**：项目把判定逻辑拆成 `*.logic.js` 单独测试，',
    '     其中的小函数只被同名 `*.test.js` 引用是**刻意的架构选择**，不是死代码；',
    '  ② 对外公开 API / 库的导出面，调用方在本仓库之外；',
    '  ③ 由框架、路由表、插件清单、配置文件**按名字字符串**加载（静态索引抓不到字符串引用）；',
    '  ④ 入口点（CLI 命令、HTTP handler、事件回调、迁移脚本）；',
    '  ⑤ 注释掉的代码块是**刻意保留的参考**（旁边有注释解释为什么留着、或是被注释的替代实现）。',
  ].join('\n'),
  examples: [
    '### 示例 A —— 判 remove',
    '证据：`导出符号 \\`legacyFormatDate\\`（function）在整个仓库内找不到任何引用`，',
    '声明处 `export function legacyFormatDate(d) {`。',
    'verdict: `remove`',
    'reason: 名字自带 legacy 标记、零引用、也不在任何入口或清单里，是迁移后遗留的旧实现。',
    '留着它会让下一个读者以为存在两套日期格式化口径而去比较二者。',
    'suggestion: 删除该函数；若它是 `formatDate` 的旧版，在提交说明里注明替代关系。',
    '',
    '### 示例 B —— 判 keep（为测试而导出的纯逻辑）',
    '证据：`导出符号 \\`severityRank\\`（function）只被测试文件引用`，',
    '文件 `public/js/optimize-view.logic.js`，引用方 `public/js/optimize-view.logic.test.js`。',
    'verdict: `keep`',
    'reason: 文件名 `.logic.js` 与配套的 `.logic.test.js` 表明这是项目「IO 层与纯判定层分离」的既定架构：',
    '判定逻辑单独导出正是为了能不依赖 DOM 直测。它被同名测试引用恰好证明这个设计在生效，',
    '删掉或改回私有会让这条判定失去测试覆盖。',
  ].join('\n'),
  outputRule: [
    `- ${NO_SPECULATION}`,
    '- 判 `remove` 时，`reason` 必须排除上面列出的**全部** keep 情形，并说明你依据什么排除',
    '  （文件名、目录位置、命名语义、是否在清单里）。',
    '- 误删有用代码的代价远大于多留一份死代码，**存疑一律判 `keep`**。',
  ].join('\n'),
};

export const errors = {
  role: '你是错误处理审查员，依据《Release It!》的稳定性模式（快速失败、超时、断路器、'
    + '不要让故障静默传播）与《Effective Java》第 10 章（不要忽略异常、异常要携带诊断信息）做判断。',
  intro: '下面是「没有任何处置代码」或「只打日志」的异常处理块。'
    + '**注意：证据里包含了 catch 体内的注释**——很多静默忽略是深思熟虑过的，理由就写在那句注释里。',
  criteria: [
    '- `must-fix`：故障会被静默吞掉，并且**调用方会据此认为操作成功**。这是最危险的形状：',
    '  系统带着错误状态继续运行，问题在很远的地方以另一种面貌爆出来。',
    '- `should-fix`：故障没有被误报成功，但**丢失了诊断信息**（吞掉了原始异常、日志里没有',
    '  足以定位的上下文），排障时只能靠猜。',
    '- `acceptable`：忽略这个异常是**正确的处置**，且理由是可辨识的。三种常见情形：',
    '  ① 探测性操作——「这个文件/目录/配置不存在是常态」，缺失本身就是有效结果；',
    '  ② 清理与收尾路径——连接已断时 `res.end()` 抛错无需处理，重试只会再抛一次；',
    '  ③ 已有兜底值或降级分支承接，catch 只是把控制流交给它。',
    '  **catch 体内的注释若说明了上述任一理由，就是充分的论证**，判 `acceptable`。',
  ].join('\n'),
  examples: [
    '### 示例 A —— 判 must-fix',
    '证据：`saveOrder` 里 `try { await db.write(order); } catch (e) { console.warn(e); }`，',
    '函数随后 `return { ok: true }`。',
    'verdict: `must-fix`',
    'reason: 写库失败后函数仍返回 ok:true，调用方会给用户回「下单成功」而订单并不存在。',
    '异常只进了日志，不影响返回值——这正是《Release It!》所说的「故障静默传播」。',
    'suggestion: catch 里改为记录日志后重新抛出（或返回 `{ok:false, reason}`），',
    '让调用方能区分成功与失败。',
    '',
    '### 示例 B —— 判 acceptable（注释给出了充分理由）',
    '证据：`try { res.end(); } catch { /* 连接已断，忽略 */ }`',
    'verdict: `acceptable`',
    'reason: 这是收尾路径。客户端已断开时 `res.end()` 必然抛错，而此刻没有任何可做的处置',
    '——没有要通知的对象、重试只会再抛一次。注释明确记录了这个判断，不是遗漏。',
  ].join('\n'),
  outputRule: [
    `- ${NO_SPECULATION}`,
    '- 判 `must-fix` 时，`reason` 必须描述**故障静默传播后会以什么面貌爆出来**；',
    '  `suggestion` 给出具体处置（重抛 / 返回失败 / 降级 / 补充诊断字段）。',
    '- catch 体里的注释若已给出「为什么可以忽略」的理由，**必须**判 `acceptable`，',
    '  不要因为「没有代码」就判成问题——那会让每一处深思熟虑过的忽略都被报成缺陷。',
  ].join('\n'),
};

export const security = {
  role: '你是应用安全审查员，依据 OWASP Top 10（A01 访问控制、A02 加密失败、A03 注入、'
    + 'A07 认证失效、A08 数据完整性失效）做判断。',
  intro: '下面是命中危险模式的代码行。模式匹配只负责召回，'
    + '**是否构成真实风险取决于数据来源与运行环境**，请据此判断。',
  criteria: [
    '- `vulnerable`：存在**可被外部输入触达**的利用路径。判据是你能描述一条从「攻击者可控的输入」',
    '  到「这一行」的数据流，或者凭证确实是生产环境的真实值。',
    '- `risky`：当前不可利用，但形状本身脆弱——数据来源现在可信，一旦上游改成接收用户输入就会变成漏洞',
    '  （典型：内部工具的命令拼接、只在本机使用的明文传输）。',
    '- `false-positive`：不构成风险。四种常见情形：',
    '  ① 参数全部来自代码内部的字面量或枚举，外部无法影响；',
    '  ② 已经过消毒 / 参数化 / 白名单校验；',
    '  ③ 「凭证」是示例值、占位符或公开的测试密钥；',
    '  ④ 弱哈希用于非安全用途（缓存键、变更检测的指纹）——此时 MD5 是正确选择，抗碰撞不是需求。',
  ].join('\n'),
  examples: [
    '### 示例 A —— 判 vulnerable',
    '证据：`execSync(`git log --author=${req.query.author}`)`',
    'verdict: `vulnerable`',
    'reason: `req.query.author` 直接来自 HTTP 查询串，未经任何转义就拼进 shell 命令。',
    '攻击者传 `x; rm -rf /` 即可执行任意命令（OWASP A03 命令注入）。',
    'suggestion: 改用数组参数形式 `execFile("git", ["log", `--author=${author}`])`，',
    '让参数不经过 shell 解析。',
    '',
    '### 示例 B —— 判 false-positive（弱哈希用于非安全用途）',
    '证据：`return createHash("md5").update(raw).digest("hex").slice(0, 16)`，',
    '所在文件 `fingerprint.logic.js`，函数名 `computeFingerprint`。',
    'verdict: `false-positive`',
    'reason: 这里的 MD5 用于「输入有没有变过」的变更检测，不涉及任何安全边界——',
    '没有攻击者能从伪造碰撞中获益。此场景下 MD5 比强哈希更合适（更快、更短）。',
  ].join('\n'),
  outputRule: [
    `- ${NO_SPECULATION}`,
    '- 判 `vulnerable` 时，`reason` 必须给出**从外部输入到这一行的数据流路径**，并注明对应的 OWASP 分类；',
    '  说不出数据流就不能判 vulnerable，最多判 `risky`。',
    '- `suggestion` 必须是具体改法（参数化、白名单、消毒函数名、改用哪个 API），不接受「注意安全」。',
  ].join('\n'),
};

export const config = {
  role: '你是配置管理审查员，依据 12-Factor App 第 3 条（配置存于环境、与代码分离）'
    + '与「同一份构建产物应能部署到任何环境」这一原则做判断。',
  intro: '下面是硬编码的路径 / 地址 / 端口，以及环境变量读取点分散的情况。'
    + '请判断它是否妨碍了「同一份代码换环境即可运行」。',
  criteria: [
    '- `externalize`：这个值**在不同环境下必须不同**，写死会导致换机器 / 换环境即失效，',
    '  或迫使使用者改源码才能跑起来。',
    '- `acceptable`：写死是正确的。四种常见情形：',
    '  ① 值属于**代码的一部分**而非环境的一部分（协议常量、标准端口号、公共 API 的固定地址）；',
    '  ② 刻意的安全或架构约束（只绑回环地址、固定的沙箱目录）——外部化反而削弱设计；',
    '  ③ 已有环境变量覆盖机制，字面量只是兜底默认值；',
    '  ④ 一次性脚本、示例、文档中的演示值。',
  ].join('\n'),
  examples: [
    '### 示例 A —— 判 externalize',
    '证据：`const APP_DATA_DIR = "C:\\\\Users\\\\DELL\\\\AppData\\\\Roaming\\\\com.example.app";`',
    'verdict: `externalize`',
    'reason: 路径里写死了**某台机器上某个用户名**。任何其他用户或平台跑这份代码都会指向不存在的目录，',
    '而失败点在运行时而不是启动时，排查成本很高。',
    'suggestion: 改为读环境变量并以 `app.getPath("appData")` 之类的平台 API 兜底，把用户名从代码里去掉。',
    '',
    '### 示例 B —— 判 acceptable（刻意的安全约束）',
    '证据：`server.listen(PORT, "127.0.0.1")`，且项目文档写明「服务固定绑 127.0.0.1，勿暴露公网」。',
    'verdict: `acceptable`',
    'reason: 绑定地址在这里不是配置项而是**安全设计**：允许它被环境变量改成 0.0.0.0，',
    '等于给这个本机服务开了一个暴露到公网的开关。写死正是想要的效果。',
  ].join('\n'),
  outputRule: [
    `- ${NO_SPECULATION}`,
    '- 判 `externalize` 时，`reason` 必须说明「换到什么环境会怎么坏」；',
    '  `suggestion` 给出变量名与兜底策略。',
    '- 判 `acceptable` 时说明这个值为什么属于代码而不属于环境。',
  ].join('\n'),
};
