---
paths:
  - 'src/**'
---

# 又大又宽的规则

这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
这一行是填充内容，用于把文件撑到 5KB 以上，模拟真实项目里那些体积大且 paths 过宽的规则文件。
